version: 2
jobs:
  build:
    docker:
      - image: ubuntu:18.04
    steps:
      - checkout
      - run:
          name: Build Dependencies
          command: make deps
      - run:
          name: Build Marlin
          command: make
